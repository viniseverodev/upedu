// Perfil do aluno — STORY-016 (Sprint 5)
// TODO: tabs: Dados | Responsáveis | Financeiro | Histórico

'use client';

export default function AlunoPerfilPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Perfil do Aluno</h1>
      {/* TODO: implementar em STORY-016 — id: {params.id} */}
    </div>
  );
}
